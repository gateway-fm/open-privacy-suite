# Video Demo Pipeline Dependencies

# HTTP client
httpx>=0.25.0

# Configuration
pyyaml>=6.0

# TTS (free, no API key required)
edge-tts>=6.1.0

# AI/ML (optional - for copy generation)
anthropic>=0.7.0

# Image processing
# Floor is 10.2.0: Aikido reports CVE-2023-50447 against the 10.0.0 the old
# floor allowed, and names 10.2.0 as the patched version.
Pillow>=10.2.0

# CLI
click>=8.1.0
rich>=13.0.0

# Async support
aiofiles>=23.0.0

# Transitive (edge-tts -> aiohttp -> multidict). Floor is 6.9.1: Aikido
# reports AIKIDO-2026-338732 against 6.8.0, which aiohttp's multidict<7,>=4.5
# range still allows, and names 6.9.1 as the patched version.
multidict>=6.9.1
